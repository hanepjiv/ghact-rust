name: Call a reusable workflow

on:
  push:
    branches: [ "main" ]

  pull_request:
    branches: [ "main" ]

jobs:
  ci-rust:
    permissions:
      issues: write
    uses: ./.github/workflows/ci-rust.yml # reuse local repository
